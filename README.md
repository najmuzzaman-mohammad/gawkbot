# Screenshots for PR #1164

Captured by `web/e2e/screenshots/publish.sh operator-edit-route 1164` from
`operator-edit-route.mjs` against vite dev with mocked broker responses.
Safe to delete after the PR merges.
