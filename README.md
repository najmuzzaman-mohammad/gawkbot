# Screenshots for PR #1163

Captured by `web/e2e/screenshots/publish.sh operator-suggested-agents 1163` from
`operator-suggested-agents.mjs` against vite dev with mocked broker responses.
Safe to delete after the PR merges.
